# assets/video/

Place project video here. Referenced by `panels/video.html`:

- `forge_intro.mp4` — project introduction video

If the file exceeds GitHub's recommended 100 MB per-file limit, host on YouTube and update `panels/video.html` to use an iframe embed instead of the `<video>` element.
